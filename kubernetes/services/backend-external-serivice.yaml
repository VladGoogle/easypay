apiVersion: v1
kind: Service
metadata:
  name: backend-external-lb
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-resource-group: "rg-easypay-rare-oarfish"
    service.beta.kubernetes.io/azure-load-balancer-ipv4: "20.36.208.244"
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  selector:
    tier: backend