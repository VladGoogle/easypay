apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
spec:
  type: NodePort
  ports:
    - name: client
      port: 3001
      targetPort: 3001
      nodePort: 30001
  selector:
    tier: frontend