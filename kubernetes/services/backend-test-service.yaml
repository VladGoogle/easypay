apiVersion: v1
kind: Service
metadata:
  name: backend-svc
spec:
  type: NodePort
  ports:
    - name: server
      port: 3000
      targetPort: 3000
      nodePort: 30002
  selector:
    tier: backend